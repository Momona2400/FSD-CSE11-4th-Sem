//console.table() method is used to display data in a tabular format in the console. It takes an array or an object as an argument and displays it as a table.

let student = {
    name: "Alice",
    age: 21,
    isEnrolled: true,
    courses: ["Math", "Science", "History"]
};

console.table(student); //displays the student object in a table format 
